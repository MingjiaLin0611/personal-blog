# 一段手写的 Markdown 笔记

这是一篇直接手写的 Markdown 文件。

它没有 `title`、`date` 或其他 frontmatter，正文就是普通 Markdown 内容。

## Markdown 能展示什么

- 标题
- 段落
- **加粗文字**
- *斜体文字*
- 列表
- 链接

例如，这是一段代码：

```ts
const message = 'Markdown works'
console.log(message)
```

文章标题、日期和标签由博客代码中的静态配置提供。
