# Personal Blog

一个使用 React、TypeScript 和 Vite 构建的个人技术博客。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
npm run preview
```

## 内容

文章位于 `content/posts/`，使用普通 Markdown 编写。文章正文由 `react-markdown` 和 `remark-gfm` 渲染；文章标题、日期和标签等页面配置位于 `src/content/posts.ts`。
